server:
  port: 80
spring:
  datasource:

    # mysql驱动类
    driver-class-name: org.gjt.mm.mysql.Driver
    url: jdbc:mysql://localhost:3306/day15homework?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: root
mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: cn.learn.learn.entity
  configuration:
    useGeneratedKeys: true
    mapUnderscoreToCamelCase: true
